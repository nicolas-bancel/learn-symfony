{% extends "base.html.twig" %}

{% block body %}

<div class="container mt-5 mb-5">
    <div class="row">
        <div class="col-md-6 text-center">
            <h1>{{ products.name }}</h1>
            <h3>{{ products.price }} €</h3>
        </div>
        <div class="col-md-6">
            <p>{{ products.description }}</p>
        </div>
    </div>

    <div class="text-center">
        <a href="{{ path('order', {'slug': products.slug }) }}" class="btn btn-info">Commander le produit</a>
    </div>
</div>

{% endblock %}